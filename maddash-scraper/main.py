#!/usr/bin/env python3
import pandas as pd
import numpy as np
from bs4 import BeautifulSoup
import sys
import requests

# Look for config file if not there: creates it, else: tries to read it
# Modi

# Ask for maddash address
# Find tests and list them
# Prompt for which one to watch
# Ask per grid which host to monitor
# Actually goes on the toolkit archive to look at the jsons. (Future proofness)
# Save config
