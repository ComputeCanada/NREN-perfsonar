The following is very specific to the Arbutus cloud. Be aware that the --local install option will
not be available to you since I made the volume containing the required RPMs and docker images myself and 
hard-coded everything I needed to mount the virtual drive on boot.
